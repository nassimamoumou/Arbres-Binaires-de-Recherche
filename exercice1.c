#include <stdio.h>

#define PUISSANCE 3
int main () {
    float nb, resultat;
    printf("saisir le nombre\n");
    scanf("%f" , nb);
    resultat = pow(nb , PUISSANCE).
    printf("Le res de l'levation a la puissance est : %f\n" , resultat);
    return 0;
}